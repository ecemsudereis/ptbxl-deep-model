"""Training layer (Person C).

trainer.py — training loop, optimizer, regularization, early stopping
metrics.py — macro-AUC + macro-F1 (CONTRACT §4)

Works WITHOUT waiting for the real components via dummy model + dummy
data.
"""
